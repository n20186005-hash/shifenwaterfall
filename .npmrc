# pnpm 11 仅从本文件读取 auth 与 registry 设置；
# 构建设置（allowBuilds / onlyBuiltDependencies 等）一律写在 pnpm-workspace.yaml。

# 放行 esbuild（astro/vite 依赖）和 workerd（wrangler 依赖）的 postinstall 脚本
# Cloudflare 构建环境需要此设置，否则 pnpm 11 会阻止这些包的二进制下载
onlyBuiltDependencies=esbuild,workerd
